services:
  api:
    command: sh -lc "corepack enable && pnpm install && pnpm -F api dev"
    volumes:
      - .:/workspace

  web:
    command: sh -lc "corepack enable && pnpm install && pnpm -F web dev --host 0.0.0.0"
    volumes:
      - .:/workspace
